# dependencies
node_modules/

# build output
dist/

# env / secrets — never commit real credentials
.env
.env.local
.env.*.local
.env.*.bak

# logs
logs/
*.log

# local Redis dump (created by `redis-server` when run locally, e.g. for
# the E2E suite in src/scripts/e2eSmokeTest.ts)
*.rdb

# OS
.DS_Store
