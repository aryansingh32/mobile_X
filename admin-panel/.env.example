# Google OAuth client ID used for admin sign-in (same OAuth client the
# mobile app uses is fine — this is a public identifier, not a secret;
# it's bundled into the client build regardless).
VITE_GOOGLE_CLIENT_ID=

# Base URL of the backend API, including the /api prefix.
VITE_API_URL=http://localhost:5000/api
